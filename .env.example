# Copy to .env.local and change before any real use.
# Signs operator sessions and file preview/download URLs. Each shop has its
# own passcode (generated at /create), but they're all signed with this one
# server secret — treat it like a real credential.
COPYSHOP_SECRET=change-me-to-a-long-random-string
